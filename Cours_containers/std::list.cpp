#include <iostream>
#include <array>
#include <algorithm>

int main(){
	//set has no front and back, we can just insert elements,
	//set and unset c bien quand tu a s de la data dupliqué et le poto stocke pas les doublons malgre que tu lui en envoies full
}